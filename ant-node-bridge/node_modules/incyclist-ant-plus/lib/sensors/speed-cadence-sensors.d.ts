import { Constants } from '../consts';
import { Profile } from '../types';
import Sensor, { SensorState } from './base-sensor';
export declare class SpeedCadenceSensorState extends SensorState {
    CadenceEventTime: number;
    CumulativeCadenceRevolutionCount: number;
    SpeedEventTime: number;
    CumulativeSpeedRevolutionCount: number;
    _CadenceUpdateTime: number;
    _SpeedUpdateTime: number;
    CalculatedCadence: number;
    CalculatedDistance: number;
    CalculatedSpeed: number;
}
export default class SpeedCadenceSensor extends Sensor {
    private states;
    wheelCircumference: number;
    getDeviceType(): number;
    getProfile(): Profile;
    getChannelConfiguration(): {
        type: string;
        transmissionType: number;
        timeout: Constants;
        period: number;
        frequency: number;
    };
    onEvent(data: Buffer): void;
    setWheelCircumference(wheelCircumference: number): void;
    onMessage(data: Buffer): void;
}
